#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <string.h>

struct DL_Node
{
    struct DL_Node* prev;
    struct Dl_Node* next;
    int data;
};

int main()
{
    struct DL_Node* head = (struct DL_Node*)malloc(sizeof(struct DL_Node));
    
}